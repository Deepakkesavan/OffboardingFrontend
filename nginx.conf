server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html index.htm;

    server_tokens off;

    location /offboardingui/ {  # ✅ Change from /emsui/
        try_files $uri $uri/ /offboardingui/index.html;  # ✅ Change here too
    }
}
